# TODO(P3-1): pin third-party actions to commit SHAs (deferred).
name: Release

# Two triggers:
# 1. Manual: Actions > Release > Run workflow > enter version (e.g. "1.0.0")
# 2. Tag push: git tag v1.0.0 && git push origin v1.0.0
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g. 1.0.0 — without 'v' prefix)"
        required: true
        type: string
      prerelease:
        description: "Mark as pre-release"
        required: false
        type: boolean
        default: false
  push:
    tags:
      - "v*"

permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/netmap-ng

jobs:
  # ──────────────────────────────────────────────
  # Prepare: determine version and create tag if needed
  # ──────────────────────────────────────────────
  prepare:
    name: Prepare Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
            TAG="v${VERSION}"
          else
            TAG="${GITHUB_REF_NAME}"
            VERSION="${TAG#v}"
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "Release: ${TAG} (${VERSION})"

      - name: Create and push tag
        if: github.event_name == 'workflow_dispatch'
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "${TAG}" -m "Release ${TAG}"
          git push origin "${TAG}"

  # ──────────────────────────────────────────────
  # Test before releasing
  # ──────────────────────────────────────────────
  test:
    name: Test
    runs-on: ubuntu-latest
    needs: prepare
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt
      - run: pip install -r backend/requirements.txt
      - run: pip install pytest pytest-asyncio httpx anyio
      - run: cd backend && python -m pytest tests/ -v --tb=short
        env:
          APP_SECRET_KEY: test-secret-key-ci-only
          AUTH_DISABLED: "true"

  # ──────────────────────────────────────────────
  # Build frontend
  # ──────────────────────────────────────────────
  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest
    needs: [prepare, test]
    permissions:
      contents: read
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run build
      - name: Archive
        run: tar -czf ../frontend-dist.tar.gz -C dist .
      - uses: actions/upload-artifact@v7
        with:
          name: frontend-dist
          path: frontend-dist.tar.gz

  # ──────────────────────────────────────────────
  # Build and push Docker images
  # ──────────────────────────────────────────────
  docker:
    name: Docker ${{ matrix.image.name }}
    runs-on: ubuntu-latest
    needs: [prepare, test]
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    outputs:
      backend-digest: ${{ steps.collect.outputs.backend-digest }}
      frontend-digest: ${{ steps.collect.outputs.frontend-digest }}
    strategy:
      matrix:
        image:
          - { name: backend, dockerfile: backend/Dockerfile, context: backend }
          - { name: frontend, dockerfile: frontend/Dockerfile, context: frontend }
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE_PREFIX }}-${{ matrix.image.name }}
          tags: |
            type=raw,value=latest
            type=semver,pattern={{version}},value=${{ needs.prepare.outputs.tag }}
            type=semver,pattern={{major}}.{{minor}},value=${{ needs.prepare.outputs.tag }}
            type=semver,pattern={{major}},value=${{ needs.prepare.outputs.tag }}

      - name: Build and push
        id: build
        uses: docker/build-push-action@v7
        with:
          context: ${{ matrix.image.context }}
          file: ${{ matrix.image.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # SBOM (SPDX-JSON) for the pushed image
      - name: Generate SBOM (SPDX-JSON)
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.IMAGE_PREFIX }}-${{ matrix.image.name }}@${{ steps.build.outputs.digest }}
          format: spdx-json
          output-file: sbom-${{ matrix.image.name }}.spdx.json
          upload-artifact: false

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v7
        with:
          name: sbom-${{ matrix.image.name }}
          path: sbom-${{ matrix.image.name }}.spdx.json

      # Build provenance attestation (SLSA)
      - name: Generate build provenance attestation
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.IMAGE_PREFIX }}-${{ matrix.image.name }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true

      # Keyless cosign signature (OIDC)
      - name: Install cosign
        uses: sigstore/cosign-installer@v4

      - name: Sign image with cosign (keyless)
        env:
          COSIGN_EXPERIMENTAL: "1"
          IMAGE: ${{ env.IMAGE_PREFIX }}-${{ matrix.image.name }}
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          cosign sign --yes "${IMAGE}@${DIGEST}"

  # ──────────────────────────────────────────────
  # Create GitHub Release with changelog + artifacts
  # ──────────────────────────────────────────────
  release:
    name: GitHub Release
    runs-on: ubuntu-latest
    needs: [prepare, build-frontend, docker]
    permissions:
      contents: write
      packages: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release
          cp artifacts/frontend-dist/frontend-dist.tar.gz release/ 2>/dev/null || true
          # Include SBOMs in the release assets if present
          for d in artifacts/sbom-*; do
            [ -d "$d" ] && cp "$d"/*.spdx.json release/ 2>/dev/null || true
          done
          cd release && sha256sum * > checksums.txt

      - name: Generate changelog
        id: changelog
        run: |
          TAG="${{ needs.prepare.outputs.tag }}"
          PREV_TAG=$(git tag --sort=-v:refname | grep -v "^${TAG}$" | head -1)
          if [ -n "$PREV_TAG" ]; then
            CHANGELOG=$(git log --pretty=format:"- %s (%h)" "${PREV_TAG}..${TAG}" -- . ':!.github')
          else
            CHANGELOG=$(git log --pretty=format:"- %s (%h)" "${TAG}" -- . ':!.github')
          fi
          # Use a random sentinel rather than literal EOF to avoid clashes with commit-message bodies.
          DELIM="EOF_$(uuidgen)"
          {
            echo "changelog<<${DELIM}"
            echo "$CHANGELOG"
            echo "${DELIM}"
          } >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.prepare.outputs.tag }}
          name: "Netmap NG ${{ needs.prepare.outputs.tag }}"
          prerelease: ${{ inputs.prerelease || false }}
          generate_release_notes: true
          body: |
            ## What's Changed

            ${{ steps.changelog.outputs.changelog }}

            ## Docker Images

            ```bash
            docker pull ${{ env.IMAGE_PREFIX }}-backend:${{ needs.prepare.outputs.version }}
            docker pull ${{ env.IMAGE_PREFIX }}-frontend:${{ needs.prepare.outputs.version }}
            ```

            Available architectures: `linux/amd64`, `linux/arm64`

            Images are signed with [cosign](https://github.com/sigstore/cosign) (keyless / OIDC) and ship with SLSA build-provenance attestations and SPDX SBOMs.

            ## Quick Start

            ```bash
            export IMAGE_TAG="${{ needs.prepare.outputs.version }}"

            curl -LO https://raw.githubusercontent.com/${{ github.repository }}/${{ needs.prepare.outputs.tag }}/docker-compose.yml
            curl -LO https://raw.githubusercontent.com/${{ github.repository }}/${{ needs.prepare.outputs.tag }}/.env.example
            cp .env.example .env
            # Edit .env with your Observium DB credentials + OAuth config

            docker compose up -d
            ```
          files: release/*
